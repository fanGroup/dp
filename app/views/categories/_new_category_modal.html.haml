.modal-dialog
  .modal-content
    .modal-header
      %button.close{"data-dismiss" => "modal", type: "button"} ×
      %h3= t('.new_category')
    = form_for(Category.new, remote: true, format: :json) do |f|
      .modal-body
        .input
          = f.label :name
          = f.text_field :name, class: "pretty-input"
      .modal-footer
        = f.submit t('.save'), class: 'modal-submit'
